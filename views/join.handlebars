<div class="list">
    {{#each data}}
        <a class="item" href="game/{{this.id}}">
            <div class="inner-item">
                {{this.name}}
            </div>        
        </a>
    {{/each}}
</div>